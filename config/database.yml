default: &default
  adapter: postgresql
  host: localhost
  user: postgres
  pool: 5
  timeout: 5000

development:
  <<: *default
  database: smashub_dev

production:
  database: smashub_prod
  url: <%= ENV['DATABASE_URL'] %>
    

test:
  <<: *default
  database: smashub_test
