default: &default
  adapter: postgresql
  host: localhost
  user: postgres
  pool: 5
  timeout: 5000

development:
  <<: *default
  database: smashub_dev

test:
  <<: *default
  database: smashub_test

production:
  url: <%= ENV['DATABASE_URL'] %>
